<!-- Banner Section -->
<%= render partial: 'movies/partial/banner-section' %>

<!-- Movies section -->
<%= render partial: 'movies/partial/movies-section', locals: { :object       => @movies,
                                                :section_name => "Фильмы",
                                                :category_url => '/category-movies'} %>

<!-- Series Section -->
<%= render partial: 'movies/partial/movies-section', locals: { :object       => @series,
                                                :section_name => "Сериалы",
                                                :category_url => '/category-series'} %>

<!-- Cartoon Section -->
<%= render partial: 'movies/partial/movies-section', locals: { :object       => @cartoons,
                                                :section_name => "Мультфильмы",
                                                :category_url => '/category-cartoons'} %>

<!-- Recommendations Section -->
<%= render partial: 'movies/partial/movies-section', locals: { :object       => @advise,
                                                :section_name => "Рекомендации",
                                                :category_url => '/category-recommendations'} %>

<!-- Genre Section -->
<%= render partial: 'movies/partial/genre-section' %>
